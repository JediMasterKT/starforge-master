#!/bin/bash
# Wrapper for StarForge CLI
# Actual implementation is in templates/bin/starforge
# This allows developers to run `bin/starforge` while keeping source in templates/

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/../templates/bin/starforge" "$@"
